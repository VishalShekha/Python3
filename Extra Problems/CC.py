# word = "Ocygen"
# word_new = word.replace('c','x')
# print(word_new)

# for number in numbers:
#     if number % 2 == 0:
#         continue
#     print(number)
#     '''In this example, the 'for' loop iterates over each number in the 'numbers' list.
# Inside the loop, there is an 'if' statement that checks if the current number is even (divisible by 2).
# If the condition is true, the 'continue' statement is executed, and the remaining code block for that iteration is skipped.
# As a result, when an even number is encountered, the 'continue' statement is triggered, and the program jumps back to the beginning of the loop for the next iteration, without executing the 'print' statement.
# Therefore, only the odd numbers in the list will be printed'''
# a = complex(input())
# # 1+3j
# print(a)

# # (1+3j)
# a.real

# # a.imag

import math

n = 25
print(math.sqrt(n))

a = 16
b = 24
print(math.gcd(a,b))

n = 1.89
print(math.floor(n))

n = 0.21
print(math.ceil(n))

x = 6
y = 3
print(math.pow(x,y))

'''You are given an integer 
�
N.
Find if it is possible to represent 
�
N as the sum of several (possibly zero) 
2
2's and several (possibly zero) 
7
7's.'''

max() - returns the highest value of a list

min() - returns the lowest value of a list

sum() - returns the sum of all the values of a list

count() - count the occurrence of a particular element

index() - returns the index of a particular element of a list

del() - delete an element at a given index
len() - checks len
set() - returns the unique elements of a list
ord() function returns the Unicode code from a given character

 
